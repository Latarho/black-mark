{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "if [ -f package-lock.json ]; then npm run build; elif [ -f web/package.json ]; then (cd web && npm run build); else npm run build; fi"
}
